# 里程碑一工程基础与数据库骨架验收记录

日期：2026-07-20
状态：本机含真实依赖验收通过，现场服务器未验证
本机验收基线提交：`2b0e5ca`（文档提交号在提交后由 Git 历史确定）

## 本机环境

- 操作系统：Ubuntu 开发环境
- GNU C++：15.2.0
- CMake：4.2.3
- libmosquitto：2.0.22（Ubuntu 包 `2.0.22-5build1`）
- libpqxx：7.10.0（Ubuntu 包 `7.10.0-2build1`）
- nlohmann/json：3.11.3
- doctest：2.4.12

## 可复现命令与结果

从仓库根目录执行：

```bash
cmake -E remove_directory route_service/build-fresh
cmake -S route_service -B route_service/build-fresh -DCMAKE_BUILD_TYPE=Debug
cmake --build route_service/build-fresh -j2
ctest --test-dir route_service/build-fresh --output-on-failure
git diff --check
```

结果摘要：CMake 清理、配置、完整构建和 CTest 退出码均为 0；CTest 共 15 项，15 项通过、0 项失败，总测试时间 0.51 秒。提交前曾只对工作树执行 `git diff --check` 并得到退出码 0，但该命令没有检查已暂存/已提交内容；独立审查随后执行 `git diff --check 2c68835..7a757cb`，退出码为 2，发现本记录原第 3、4 行的 Markdown 尾随空白。本次文档修复后重新对完整提交范围与工作树检查，结果见“审查修复复验”，不沿用原不完整证据。

版本采集命令与实际退出码：

```bash
c++ --version | head -n 1                                      # 退出码 0
cmake --version | head -n 1                                     # 退出码 0
pkg-config --modversion libmosquitto libpqxx                     # 退出码 0
pkg-config --modversion nlohmann_json doctest                    # 退出码 1
pkg-config --modversion nlohmann_json                            # 退出码 0
dpkg-query -W -f='${Package} ${Version}\n' doctest-dev \
  nlohmann-json3-dev libmosquitto-dev libpqxx-dev                # 退出码 0
```

首次复合采集 `pkg-config --modversion nlohmann_json doctest` 因 Ubuntu 的 doctest 不提供 `.pc` 元数据而退出 1；随后分别使用 pkg-config 与 `dpkg-query` 成功确认 nlohmann/json 3.11.3 和 doctest 2.4.12。版本采集失败不影响已独立完成的构建与测试结果。

## 设计第 14 节逐条验收

| 验收项 | 本机结果与证据 |
|---|---|
| 单一可执行程序 | 通过。CMake 仅定义一个部署目标 `route_service`；`route_core`、`route_adapters`、`pqxx_compat` 为内部库，其余可执行目标均为测试。 |
| 严格配置 | 通过。`app_config` 覆盖未知/缺失字段、类型、范围、重连关系与凭据保护；命令行覆盖必填、未知、重复和冲突参数。 |
| 日志格式 | 通过。`logger` 覆盖本地毫秒时间、固定宽度英文等级、中文单行清理、stdout/stderr 分流与异常时间占位符。 |
| 有界队列 | 通过。覆盖非阻塞满队列、FIFO、关闭排空、关闭唤醒、并发和仅移动类型；测试辅助已补全 `waitpid`/`kill` 的 `EINTR` 与回收检查。 |
| 显式、只向前迁移 | 通过。仅 `--migrate-only` 生成执行计划，逐迁移事务执行，无 down、DROP 或 TRUNCATE 路径。 |
| 正常模式只检查 | 通过。运行编排测试直接断言使用“规划检查”且绝不执行迁移。 |
| 无级联删除 | 通过。SQL 静态契约禁止 `ON DELETE CASCADE`，且未建立 `source_school_permissions`。 |
| 数据库失败退出 | 通过。运行编排在 PostgreSQL 连接或迁移检查失败时返回非零且不创建 MQTT。 |
| MQTT 自动重连 | 本机边界测试通过。覆盖 ERRNO 交由网络循环、EAI 指数退避、停止中断、失败消费后重新 Start 及三类运行状态；本机真实 Broker 连接已验证，主动中断 Broker 后的恢复仍未验证。 |
| 无第二里程碑业务 | 通过。`src/` 未出现 registration、telemetry、业务订阅/发布或配置/飞控路由实现。 |

本轮同时闭环任务 7 的 CMake 诊断：平台检查先于 `readelf` 查找，缺失时输出中文依赖说明；GNU ld 探测不匹配时同时输出整理后的 stdout 与 stderr。任务 8/9 实施报告已同步 log 回调、重试 Start 断言、迁移规划模式和运行状态覆盖范围。

## 本机真实依赖与树莓派联调

因现场硬件服务器和公网穿透暂不可用，经用户确认将本轮外部依赖验收临时转移到开发机；该结果不替代后续现场服务器验收。

- PostgreSQL 18.4 的 `18/main` 集群在本机 `127.0.0.1:5432` 运行。
- 创建非超级用户、无建库和建角色权限的专用登录角色 `cns_route`，以及由其拥有的全新空数据库 `cns`；随机密码仅保存在 Git 忽略目录的 `0600` 本机配置中，本记录不保存密码。
- 迁移前以 `cns_route` 验证 public 表数量为 0。第一次 `--migrate-only` 应用版本 1“创建迁移版本表”和版本 2“创建核心业务表”；第二次执行没有重复应用迁移，两次退出码均为 0。
- 最终数据库含 `schema_migrations`、`schools`、`devices`、`device_latest_states`、`command_sources`、`commands`，迁移记录为版本 1、2。
- 使用临时 Mosquitto 2.0.22 监听开发机局域网 `192.168.10.211:18884`，只供本轮匿名联调，不修改系统 `1883` 服务。
- 树莓派 `192.168.11.4` 以客户端 `cns-rpi-DCDWCNS1S2MEAG1VTA0C` 连接成功；实际 topic namespace 为 `cns_rpi`。registration 为 QoS 2 retained，telemetry 为 QoS 1 retained 且约每秒发布，配置和飞控命令 topic 均完成 QoS 2 订阅，Last Will 为 registration QoS 2 retained。
- 实测 registration 包含 `vendor_id=DCDWCNS1S2MEAG1VTA0C`、`dcdw_label=DCDW-001`、`school_name=SEU` 和 `status=online`；最新 telemetry JSON 可解析并包含 identity、drone_id、telemetry、modules、alarms 和 logs。
- `route_service` 正常模式连接 PostgreSQL 后通过客户端 `cns-route-service-local-m1` 连接同一真实 Broker，输出“路由服务已启动”和“MQTT连接已恢复”。向精确进程发送 SIGTERM 后输出“路由服务已停止”，退出码为 0。
- 设备首次未连接的原因经 10 秒非侵入式系统调用跟踪确认：USB5 持续收到合法 MAVLink 数据，但当时没有消息 ID 12900 的 `OPEN_DRONE_ID_BASIC_ID`；固件侧随后确认身份帧发送与 Remote ID 模块在位耦合。模块恢复后身份帧到达，MQTT 随即连接。该固件耦合不在本仓库里程碑一修改范围内。

## 现场待授权操作清单

以下项目均为“未验证”，本次没有连接 SSH、没有安装包、没有创建数据库或角色、没有写现场配置、没有启动现场进程，也没有改变远端状态。

获授权后拟按以下准确范围操作；服务器仓库路径、远端地址与目标提交必须先由用户确认，不自行假设：

1. 只读检查：在用户确认的服务器仓库路径执行 `git status --short --branch`、`git remote -v`、`git log -1 --oneline`，确认工作树、远端和待拉取提交。
2. 依赖：仅安装实际缺失的 `build-essential`、`cmake`、`pkg-config`、`binutils`、`libmosquitto-dev`、`libpqxx-dev`、`nlohmann-json3-dev`、`doctest-dev`、`postgresql`、`postgresql-client`；不修改 Mosquitto 配置。
3. PostgreSQL：创建专用角色 `cns_route` 和数据库 `cns`，密码在现场随机生成，绝不写入仓库或验收记录；创建前先确认同名对象不存在或由用户决定复用策略。
4. Broker 只读确认：先读取用户指明的现有 Mosquitto 配置与监听状态，确认实际地址、端口及匿名/用户名密码/TLS 策略；不修改 Broker。将确认结果交由用户批准后，才写入现场 JSON，不预设任何地址、端口或认证状态。
5. 配置：写入 `/etc/cns/route_service.json`，所有者和组由用户确认，权限 `0600`；后续迁移与正常模式必须以同一专用服务账户运行，确保配置可读且不以 root 运行服务。
6. 数据安全门：迁移前检查目标数据库是否为本里程碑新建的空数据库；若已有数据立即停止并请求恢复路径确认。不得执行 `DROP DATABASE`、`DROP TABLE`、`TRUNCATE` 或清空已有数据的测试。
7. 构建：在用户确认的仓库路径执行 Debug CMake 配置、`cmake --build ... -j2` 与 CTest。
8. 迁移：使用用户确认的专用服务账户和绝对路径执行两次 `route_service --config /etc/cns/route_service.json --migrations <用户确认仓库路径>/route_service/migrations --migrate-only`，验证首次应用和第二次幂等跳过。
9. 临时进程：以同一专用服务账户前台运行正常模式，不创建 systemd 单元；观察 MQTT 连接，临时中断/恢复 Broker 仅在用户明确额外授权后执行；发送 SIGTERM 并确认有序退出。

## 审查修复复验

本节由本次文档修复完成后按实际命令结果填写，检查范围包括 `2c68835..HEAD` 的完整提交差异和未提交工作树，避免再次把局部工作树检查误写成完整提交检查。

提交本次修复前执行：

```bash
git diff --check 2c68835
git diff --check
```

第一条检查从 `2c68835` 到当前工作树的完整有效差异，退出码 0；第二条检查未提交工作树，退出码 0。文档提交后还需以新 `HEAD` 重新执行 `git diff --check 2c68835..HEAD` 和 `git diff --check`，最终结果记录在任务 10 实施报告中。

## 未验证项与不得修改的现场状态

- 本机已验证：PostgreSQL 连接、空库首次迁移、二次幂等迁移、正常模式迁移一致性检查、真实 Mosquitto 连接、真实树莓派 registration/telemetry，以及 SIGTERM 端到端退出。
- 仍未验证：现场服务器依赖版本、现场 PostgreSQL 和 Mosquitto 集成、主动中断 Broker 后自动重连，以及 systemd 部署；这些项目保留到现场恢复后执行。
- 不得修改：服务器已有仓库未提交内容、既有 Git 远端、已有 PostgreSQL 数据库/角色/数据、Mosquitto 配置与运行策略、systemd 单元、现场凭据及其他服务进程。
- 未获用户授权前保持停止；当前结论不等同于现场验收通过。
