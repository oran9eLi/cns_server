# MQTT 发布完成 MID 误报修复实施计划

> **供智能代理执行：** 必须使用 `superpowers:executing-plans` 按任务执行；本修复不使用子代理。步骤使用复选框跟踪。

**目标：** 正确关联状态事件和来源 ACK 的发布完成 MID，消除周期性误报，同时保留真正未知或重复 MID 的错误检测。

**架构：** MQTT 适配层保留配置命令的 token 双向映射，另用一个集合登记不需要业务完成通知的发布 MID。发布调用与 MID 登记在同一把回调状态互斥锁内完成，发布完成回调按命令 MID、无需处理 MID、未知 MID 三类分流。

**技术栈：** C++23、libmosquitto 2.x、doctest、CMake/CTest。

## 全局约束

- 所有代码注释、测试名称、日志和提交说明正文使用中文，Git 提交前缀使用英文。
- 不改变 MQTT topic、QoS、retain、payload、数据库结构、命令状态机或 ACK 协议。
- 真正未登记或重复完成的 MID 继续输出原有限频错误。
- 不停止当前服务器、Broker 或树莓派；真实观察由用户控制进程启停。
- 保留仓库中用户未跟踪的 `.vscode/`，不加入提交。

---

### 任务 1：补齐无需业务通知发布的 MID 生命周期

**文件：**

- 修改：`route_service/tests/test_mqtt_commands.cpp`
- 修改：`route_service/src/adapters/mqtt/mqtt_client.cpp`
- 验证：`route_service/tests/test_mqtt_business.cpp`

**接口：**

- 使用：`MqttClient::PublishSourceConfigAck(std::string_view, std::string_view)`
- 使用：`MqttClient::PublishStateEvent(std::string_view, std::string_view)`
- 保持：`MqttClient::PublishConfigSet(std::uint64_t, std::string_view, std::string_view)` 的容量和完成通知语义不变。
- 产生：`CallbackState::untracked_completion_mids`，保存无需向业务层通知的在途 MID。

- [ ] **步骤 1：写出状态事件、重复 MID 和断线清理的失败测试**

在 `test_mqtt_commands.cpp` 中扩展现有发布测试，使状态事件也通过带 MID 的注入器发布并触发回调：

```cpp
TEST_CASE("来源ACK和状态事件完成不误报未知MID") {
  std::ostringstream out;
  std::ostringstream err;
  cns::logging::Logger logger(cns::logging::Level::kDebug, out, err);
  Injection injection;
  ScopedInjection scoped{injection};
  auto client = MakeClient(logger);

  REQUIRE(client->PublishSourceConfigAck("cns/sources/source/config/ack", "{}")
              .has_value());
  REQUIRE(client->PublishStateEvent("cns/events/devices/id/state", "{}")
              .has_value());
  REQUIRE(injection.publish_callback != nullptr);
  for (const auto& publication : injection.publications) {
    injection.publish_callback(nullptr, injection.context,
                               std::get<0>(publication));
  }
  CHECK(err.str().find("未知或重复") == std::string::npos);
}

TEST_CASE("未知和重复MID仍记录错误") {
  {
    std::ostringstream out;
    std::ostringstream err;
    cns::logging::Logger logger(cns::logging::Level::kDebug, out, err);
    Injection injection;
    ScopedInjection scoped{injection};
    auto client = MakeClient(logger);
    REQUIRE(injection.publish_callback != nullptr);
    injection.publish_callback(nullptr, injection.context, 999);
    CHECK(err.str().find("未知或重复") != std::string::npos);
  }
  {
    std::ostringstream out;
    std::ostringstream err;
    cns::logging::Logger logger(cns::logging::Level::kDebug, out, err);
    Injection injection;
    ScopedInjection scoped{injection};
    auto client = MakeClient(logger);
    REQUIRE(client->PublishSourceConfigAck("cns/sources/source/config/ack", "{}")
                .has_value());
    REQUIRE(injection.publish_callback != nullptr);
    const int mid = std::get<0>(injection.publications.front());
    injection.publish_callback(nullptr, injection.context, mid);
    injection.publish_callback(nullptr, injection.context, mid);
    CHECK(err.str().find("未知或重复") != std::string::npos);
  }
}

TEST_CASE("断线清理无需业务通知的在途MID") {
  std::ostringstream out;
  std::ostringstream err;
  cns::logging::Logger logger(cns::logging::Level::kDebug, out, err);
  Injection injection;
  ScopedInjection scoped{injection};
  auto client = MakeClient(logger);
  REQUIRE(client->PublishStateEvent("cns/events/devices/id/state", "{}")
              .has_value());
  const int mid = std::get<0>(injection.publications.front());
  injection.disconnect_callback(nullptr, injection.context, MOSQ_ERR_CONN_LOST);
  injection.publish_callback(nullptr, injection.context, mid);
  CHECK(err.str().find("未知或重复") != std::string::npos);
}
```

测试中使用两个独立客户端验证“未知”和“重复”，保证 30 秒限流器不会遮蔽第二个场景。断线测试使用 `MOSQ_ERR_CONN_LOST`，并检查错误流包含 `未知或重复`。

- [ ] **步骤 2：运行定向测试并确认失败原因**

```bash
cmake -S route_service -B build/route-service -DCMAKE_BUILD_TYPE=Debug
cmake --build build/route-service --target route_service_mqtt_command_tests -j2
ctest --test-dir build/route-service -R route_service_mqtt_command_tests --output-on-failure
```

预期：测试失败；状态事件完成被记录为未知 MID，证明复现了现场误报。

- [ ] **步骤 3：实现最小 MID 分类与竞态防护**

在 `mqtt_client.cpp` 中：

1. 将 `source_ack_mids` 重命名为 `untracked_completion_mids`，同步修改回调放弃、重新配置和断线清理点。
2. `PublishSourceConfigAck` 和 `PublishStateEvent` 在调用 `mosquitto_publish()` 前获取 `callback_state_->mutex`，发布成功后将返回 MID 插入集合。
3. `PublishStateEvent` 从传入空 MID 指针改为传入 `&mid`。
4. `HandlePublish` 在找不到命令 MID 时删除 `untracked_completion_mids` 中的 MID；删除数量为零时才标记为未知。

核心结构保持如下：

```cpp
std::unordered_set<int> untracked_completion_mids;

std::lock_guard lock{callback_state_->mutex};
int mid = 0;
const int result = mosquitto_publish(client_, &mid, topic_value.c_str(),
                                     payload_size, payload.data(), qos, false);
if (result != MOSQ_ERR_SUCCESS) return std::unexpected(...);
callback_state_->untracked_completion_mids.insert(mid);
```

`topic_value` 和 `payload_size` 在调用前构造并校验，错误信息继续使用现有中文文本。不要把无需业务通知的 MID 计入 `publish_capacity`。

- [ ] **步骤 4：运行定向测试并确认通过**

```bash
cmake --build build/route-service --target route_service_mqtt_command_tests route_service_mqtt_business_tests -j2
ctest --test-dir build/route-service -R 'route_service_mqtt_(command|business)_tests' --output-on-failure
```

预期：两个 MQTT 测试目标全部通过；现有 QoS、retain、命令容量和完成一次性断言保持通过。

- [ ] **步骤 5：运行全量验证**

```bash
cmake --build build/route-service -j2
ctest --test-dir build/route-service --output-on-failure
git diff --check
```

预期：全部测试通过，`git diff --check` 无输出。

- [ ] **步骤 6：提交代码修复**

```bash
git add -- route_service/src/adapters/mqtt/mqtt_client.cpp route_service/tests/test_mqtt_commands.cpp
git commit -m "fix: 修复MQTT发布完成MID误报"
```

提交不得包含 `.vscode/` 或运行时配置。

- [ ] **步骤 7：执行真实链路观察验收**

在用户保持当前 Broker、树莓派和前台 route_service 运行的前提下，使用修复后的新进程持续接收真实遥测至少 35 秒：

```text
验收通过条件：设备状态事件继续发布，route_service 日志中不再出现
“MQTT收到未知或重复的发布完成MID”。
```

另外用单元测试保留真正未知和重复 MID 的错误检测，不为真实验收构造破坏性 MQTT 流量。
