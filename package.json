{
  "name": "cns-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "backend_service/protocol",
    "backend_service"
  ],
  "scripts": {
    "build": "npm run build -w @cns/backend-protocol && npm run build -w backend_service",
    "typecheck": "npm run typecheck -w @cns/backend-protocol && npm run typecheck -w backend_service",
    "test": "npm run build -w @cns/backend-protocol && npm run test -w backend_service",
    "dev:backend": "npm run build -w @cns/backend-protocol && npm run dev -w backend_service",
    "dev": "npm run dev:backend"
  },
  "engines": {
    "node": ">=20.11 <25",
    "npm": ">=10"
  }
}
